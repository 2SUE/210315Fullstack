<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sue.spring.board.BoardMapper">
    <!-- param 객체 주소값에 멤버 필드인 iboard에 auto-increament인 iboard 값 넣어줌 -->
    <insert id="insBoard" useGeneratedKeys="true" keyProperty="iboard">
        INSERT INTO t_board
        ( title, ctnt, iuser )
        VALUES
        ( #{title}, #{ctnt}, #{iuser} )
    </insert>

    <!-- SELECT는 resultType 써줘야댐 -->
    <select id="selBoardList" resultType="BoardDomain">
        SELECT A.iboard, A.title, A.iuser, A.regdt, B.unm AS writerNm, B.profileImg
        FROM t_board A
        INNER JOIN t_user B
        ON A.iuser = B.iuser
        ORDER BY A.iboard DESC
    </select>

    <select id="selBoard" resultType="BoardDomain">
        SELECT A.iboard, A.title, A.ctnt, A.iuser, A.regdt, B.unm AS writerNm, B.profileImg
        FROM t_board A
        INNER JOIN t_user B
        ON A.iuser = B.iuser
        WHERE A.iboard = #{iboard}
    </select>
</mapper>